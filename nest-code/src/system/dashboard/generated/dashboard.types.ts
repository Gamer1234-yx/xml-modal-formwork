/**
 * 首页 可复用的参数类型和返回类型
 * 自动生成 - 来源：dashboard.xml
 * ⚠️ 此文件每次重新生成都会被覆盖
 */

export interface DashboardSummary {
  userCount?: number;
  productCount?: number;
  modelCount?: number;
  systemModuleCount?: number;
}
